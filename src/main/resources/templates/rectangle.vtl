<p><h3>Your rectangle's length is $newRectangle.getLength()</h3></p>
<p><h3>Your rectangle's width is $newRectangle.getWidth()</h3></p>

#if ($newRectangle.isSquare())
  <p><h3>Your rectangle is a square!</h3></p>
  <h4>Surface area of a cube with those dimensions: $myCube.surfaceArea()</h4>
  <h4>Volume of a cube with those dimensions: $myCube.volume()</h4>
#else
  <p><h3>Your rectangle isn't a square!</h3></p>
#end
